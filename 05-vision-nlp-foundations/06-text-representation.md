# Module 05.6 — Text Representation Before Transformers

## Capability
Understand sparse/count-based baselines and why token representation matters.

## Mental model / core ideas
Bag-of-words/TF-IDF discard order but provide strong, interpretable baselines for many tasks.

## Practice
Build a TF-IDF classifier and compare to a naive keyword baseline.

## Debugging / transfer task
Detect leakage from metadata or duplicated documents.

## Build evidence
Produce an NLP baseline report.

## Mastery check
Explain when a simple sparse baseline is preferable to an LLM.

**Rule:** if you can execute the recipe but cannot explain the failure modes or adapt it to a different dataset/system, keep practicing.
