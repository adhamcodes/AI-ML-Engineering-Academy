# Phase 5 Assessment — Representation Choice

Given a new vision or text problem, establish a simple baseline and compare it with a learned representation approach. Explain preprocessing, representation assumptions, metric choice, error clusters, compute/complexity cost and why a more complex model is or is not justified.
