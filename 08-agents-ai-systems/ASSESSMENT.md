# Phase 8 Assessment — Controlled Agent

Given an unfamiliar tool set, design the least-agentic architecture that satisfies the task. Implement state, permission boundaries, maximum steps, recovery, one human-approval point, tracing and trajectory evals. The grader injects a failed tool, repeated-call trap and malicious tool/content input.
