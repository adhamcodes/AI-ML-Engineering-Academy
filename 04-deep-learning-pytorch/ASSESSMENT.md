# Phase 4 Assessment — Deep Learning Builder

**Mode:** closed assistance for the core diagnosis/repair; official PyTorch documentation allowed.

You receive an unfamiliar PyTorch repository with several interacting defects. Establish a simple baseline, prove the data/splits are sane, overfit a tiny sample, inspect gradients/activations, repair training and evaluation, add reproducibility/checkpoint evidence, and produce a short root-cause report.

## Pass standard
The model must train and evaluate correctly, but code execution alone is insufficient. You must explain the observed symptoms, tests used to discriminate hypotheses, why each fix worked, and one remaining limitation.
