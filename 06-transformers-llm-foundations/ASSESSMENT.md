# Phase 6 Assessment — Transformer Foundations

Closed-assistance core. Trace tokenization→embeddings→attention→block→logits, calculate/inspect a small attention example, repair a broken causal mask/target shift, compare decoding strategies, and explain whether prompting, RAG or fine-tuning would address three different failure scenarios.
