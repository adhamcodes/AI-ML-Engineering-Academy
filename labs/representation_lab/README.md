# Representation Lab

Choose one tiny image task and one tiny text task.

For each, compare two representations before changing the model:

- image: raw flattened pixels vs a representation that preserves local spatial structure,
- text: raw token IDs/counts vs a representation that captures more useful context.

Keep the dataset/model deliberately small. The goal is to explain how representation changes what patterns a model can learn, not to win a benchmark.
